#include <stdio.h>
/**
 * main - print alphabet in lowercase
 *
 * Return: 0 zero
 */
int main(void)
{
	char letter = 'a' /*Declaring statement */
		for(letter= 'a'; letter <= 'z'; letter++)
			putchar(letter);
	putchar('\n');
	return(0);
}
